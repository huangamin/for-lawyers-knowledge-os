# SOP｜Knowledge OS 多设备同步与新电脑接入

## 1. 唯一 Source of Truth

云端主库（如 Google Drive My Drive / 其他同步盘）为唯一主库；各设备本地路径只是"窗口"。禁止另建并行副本。

## 2. 工作原理

设备 A ⇄ 本地挂载 ⇄ 云端主库 ⇄ 本地挂载 ⇄ 设备 B；谁保存，云端更新，另一台自动拉到。

## 3. 新电脑接入 14 步

1. 安装同步工具；2. 登录同一账号；3. 同步主库；4. 确认文件完整；5. 文件夹设为"始终保留在此设备上"；6. 安装 Obsidian；7. Open folder as vault；8. 检查 .obsidian；9. 检查插件；10. 检查 Dashboard/Dataview/Bases；11. 安装 Codex；12. 连接 Knowledge OS（CLAUDE.md 随库自动生效）；13. 执行同步测试；14. 正式使用。

每一步判断成功标准：见各步描述（文件可打开、插件启用、查询渲染、双向修改同步）。

## 4. 配置同步规则

随库同步：plugins / community-plugins / core-plugins / appearance / graph / hotkeys / app。
设备特定（后关者覆盖，不影响数据）：workspace.json、插件运行缓存。

## 5. 日常规范

同一时间只在一台设备编辑；保存后等同步图标转完再切换；不制造副本。

## 6. 冲突处理

出现冲突副本 → 逐段比对合并 → 删除冲突副本（删除会同步）→ 记 log。

## 7. 备份与恢复（Sync ≠ Backup）

三层：①实时同步（含回收站/版本历史）；②独立归档（如 Notion/离线导出）；③定期离线快照。误删：回收站恢复；改坏：版本历史；全库灾难：从独立归档恢复。
